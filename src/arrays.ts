const array1: string[] = [];
array1.push("Tonny");

console.log(array1.includes("Tonny"));

array1.forEach((item) => {
  console.log(item);
});

const booleans: Array<Boolean> = [];
booleans.push(true);
booleans.push(true);
booleans.push(false);
booleans.push(true);

console.log(booleans);
